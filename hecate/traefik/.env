# Must match this host's /etc/hermes-cli/config.yml registry_path (set via
# `hc init --registry-path`) -- lets the container see the whole registry
# clone at the same absolute path so hc sync's symlinks resolve.
REGISTRY_PATH=/opt/infra/hermes
